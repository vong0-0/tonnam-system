VITE_API_URL=http://localhost:3000

# Base prefix prepended to bare menu-image filenames stored in the DB.
# Leave empty if image_url already holds full URLs.
# Dev: the API serves menu photos here → http://localhost:8080/uploads/menu
VITE_IMAGE_BASE_URL=
